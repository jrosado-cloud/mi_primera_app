import 'package:flutter/material.dart';

class AlicuotaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Estado de Alícuota')),
      body: Center(child: Text('Aquí verás tus pagos de mayo.')),
    );
  }
}